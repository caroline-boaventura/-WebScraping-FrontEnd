import { Box, styled } from "@mui/material";

export const SearchContainer = styled(Box)({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    padding: '4rem',

    'button': {
        marginLeft: '2rem'
    },
})